int clk = 12;
int dt = 13;
int yellow = 8;
int red = 9;

void setup() {
  // put your setup code here, to run once:
  pinMode(dt, OUTPUT);
  pinMode(clk, OUTPUT);
  pinMode(red, OUTPUT);
  pinMode(yellow, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:

}
